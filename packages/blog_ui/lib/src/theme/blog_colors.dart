import 'dart:ui';

/// Class to hold color values for the Blog UI.
abstract class BlogColors {
  /// Seed white color for the UI.
  static const Color seedWhite = Color(0xFFFAFAFA);

  /// Seed blue-grey color for the UI.
  static const Color seedBlueGrey = Color(0xFF607D8B);
}
