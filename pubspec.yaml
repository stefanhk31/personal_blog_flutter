name: personal_blog_flutter
description: A Very Good Project created by Very Good CLI.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  api_client:
    path: packages/api_client
  bloc: ^8.1.2
  blog_repository:
    path: packages/blog_repository
  blog_ui:
    path: packages/blog_ui
  butter_cms_client:
    path: packages/butter_cms_client
  equatable: ^2.0.5
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  flutter_localizations:
    sdk: flutter
  http: ^1.2.0
  intl: ^0.18.0

dev_dependencies:
  bloc_test: ^9.1.2
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.3
  network_image_mock: ^2.0.1
  very_good_analysis: ^5.0.0

flutter:
  uses-material-design: true
  generate: true
